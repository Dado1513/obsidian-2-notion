
```bash
uv run python obsidian_to_notion.py --api-key "<api_key_notion>" --database-id "<notion_db>" --vault-dir "<vault_dir_obsidian>"  --provider github  --github-token "<github_token>" --github-repo-owner "<username>" --github-repo-name "<github_repo_name_for_foto>"
```